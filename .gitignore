# Rendered configs and anything holding the PSK or a device backup
vars.env
*.rendered
*.rsc
!*.rsc.tmpl
backups/
*.xml

# Terraform: state and real variable files never belong in a public repo
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform/
.terraform.lock.hcl
tfplan
